<% if @section %>
  <div id="section_menu_container">
    <ul id="section_menu" class="menu">
      <!-- TODO instead, have the controller register the actions -->
      <li><%= link_to_section_main_action(@site, @section) %></li>
      <% if @section.accept_comments? %>
        <li><%= link_to 'Comments', admin_comments_path(:section_id => @section) %></li>
      <% end %>
      <% if @section.instance_of?(Section) || @section.instance_of?(Blog) || @section.instance_of?(Wiki) %>
        <li><%= link_to 'Categories', admin_categories_path(@site, @section) %></li>
      <% end %>
      <li><%= link_to 'Settings', edit_admin_section_path(@site, @section) %></li>
    </ul>
  </div>
<% end %>
