<% 
	table_for @categories do |t|
		t.column :category, :actions

		# t.head.row do |r|
		# 	r.cell t(:'adva.categories.total_with_count', :count => @categories.total_entries), :class => :total, :colspan => :all
		# end

		t.row do |r, category|
			r.cell link_to_edit(category.title, category, :class => 'category')
			r.cell link_to_edit(category) + "\n" +
			       link_to_delete(category)
		end

		t.empty :p, :class => 'empty' do
	    t(:'adva.categories.no_categories_in_section', :section => @section.title) + "\n" +
			link_to_new(:'adva.categories.links.create_now', [@section, :category])
		end
	end 
%>
