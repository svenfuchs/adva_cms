<% content_for :action_nav do %>
  <ul class="sub-nav clearfix">
    <li><%= link_to content_tag(:span, 'Plugins'), admin_plugins_path(@site) %></li>
    <% if @plugin %>
      <li><%= link_to content_tag(:span, 'Restore Defaults'), admin_plugin_path(@site, @plugin), :confirm => "Are you sure you wish to delete this site?", :method => :delete %></li>
    <% end %>
  </ul>
<% end %>
