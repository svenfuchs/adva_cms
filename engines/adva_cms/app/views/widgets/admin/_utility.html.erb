<div id="utility-links" class="clearfix">
  <!-- 
  <ul class="breadcrumb">
    <li><a href="#">Dashboard</a> &raquo;</li>
    <li><a href="#">Accounts</a> &raquo;</li>
    <li class="active"><a href="#">small Account</a></li>
  </ul>
  -->
  <ul class="userlinks">
    <% if current_user %>
      <li>Logged in as: <%= link_to_profile @site, :name => current_user.name %></li>
      <li <% unless @site %>class="last"<% end %>><%= link_to 'Logout', logout_path, :method => :delete %></li>
    <% else %>
      <li>You're not logged in</li> 
      <li <% unless @site %>class="last"<% end %>><%= link_to 'Log In', :controller => 'session', :action => 'new' %></li>    
    <% end %>
    <% if @site %>
      <li class="last"><%= link_to 'Website »', "http://#{@site.host}" %></li>
    <% end %>
  </ul>
</div>

