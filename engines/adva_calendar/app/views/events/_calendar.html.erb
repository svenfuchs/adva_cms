<% date = current_timespan[0]
puts y @section.days_in_month_with_events(date)
%>
<%= calendar(:year => date.year, :month => date.month) do |day|
  puts y day
  if @section.days_in_month_with_events(date).include?(day)
    [link_to( day.mday, calendar_events_path(:year => date.year, :month => date.month, :day => day.mday)), {:class => 'specialDay'}]
  else
    [day.mday, {:class => "normalDay"}]
  end
end
%>