<%% if collection_title(@category, @tags) %>
  <h2 class="list-header"><%%= collection_title(@category, @tags) %></h2>
<%% end %>

<%%= render :partial => 'article', :collection => @articles, :locals => {:mode => :many} %>
<%%= render :partial => 'footer' %>