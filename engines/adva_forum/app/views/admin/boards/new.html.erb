<h2><%= t(:'adva.boards.titles.new') %></h2>
  
<% form_for :board, @board, :url => admin_boards_path do |f| %>

    <%= render :partial => 'form', :locals => {:f => f} %>

	<p class="buttons">
	  <%= submit_tag t(:'adva.common.save') %> <%= t(:'adva.common.connector.or') %>
	  <%= link_to t(:'adva.common.cancel'), admin_boards_path(@site, @board.section) %>
	</p>
<% end %>