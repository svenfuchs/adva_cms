<% 
  content_for :sidebar do 
    f.tabs do
      f.tab :options do
		    f.select(:filter, filter_options, {}, :class => 'columnsFilter', :label => :'adva.titles.filter')
      end
			f.tab :assets do 
		  	assets = { :latest => @site.assets.recent, :attached => [], :bucket => @site.assets.bucket(session[:bucket]) }
		  	render "admin/assets/widget/widget", :assets => assets
			end if Rails.plugin?(:adva_assets)
    end
  end
%>
