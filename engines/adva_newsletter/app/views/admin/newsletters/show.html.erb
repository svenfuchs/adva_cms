<% content_for :sidebar do %>
  <%= render :partial => 'admin/issues/sidebar', :locals => {:newsletter => @newsletter} %>
<% end %>

<h2><%= h(@newsletter.title) %></h2>
<p><%= h(@newsletter.desc) %></p>
<ul>
  <li>Subscriptions: <%= @newsletter.subscriptions_count %></li>
</ul>

<% if @newsletter.issues.present? %>
  <table id="issues" class="list" cellspacing='0'>
    <thead>
      <tr>
        <th scope="col">Issues</th>
      </tr>
    </thead>
    <tbody>
      <% @newsletter.issues.each do |issue| %>
        <tr>
          <td><%= link_to h(issue.title), admin_issue_path(@site, @newsletter, issue) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <div class="empty">
    <%= t('adva.issues.no_issues') %>
    <%= link_to t('adva.issues.new'), new_admin_issue_path(@site, @newsletter) %>
  </div>
<% end %>
